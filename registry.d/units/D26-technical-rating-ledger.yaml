# Unit descriptor — data collector unit D26 (technical-rating ledger).
#
# `console-policy` §2.6: onboarding a module is writing THIS FILE, committed
# beside the thing it describes and pointing at where its facts ALREADY live.
# Nothing about the console changes to render this unit.
#
# `data_collection_plan_260914.md` §4.1: `data_gate/clauses.py` GENERATES every
# clause from these descriptors. Nothing is hand-listed (`observability-policy`
# §2.2) — a hand-maintained monitored-things list drifts, and its drift is
# invisible because the missing rows produce no signal.
#
# RED BY DEFAULT. The `audit.cells` block below is the 2026-09-14 baseline
# reading, transcribed from alpha-engine-config/private-docs/data_collection_audit_260914.md §3. It is NOT evidence: every cell, including
# the ones scored PRESENT, is a clause that is RED until the gate measures it,
# and a first reading that DISAGREES with its cell is itself a finding — either
# the audit was wrong or the system has drifted since.
#
# PUBLIC REPO. This file carries key templates, triggers and consumer paths that
# are already public in this repository's own code. IAM role names, alarm
# thresholds and runbook detail stay in `nous-ergon-ops` (plan §5).
unit_id: D26
component_id: "data-collector-d26-technical-rating-ledger"
owning_repo: "nousergon-data"
title: "technical-rating ledger"
component: 1
lifecycle: "in-service"
owner: brian
code_path: "collectors/technical_rating_ledger.py:145"
trigger:
  kind: "step-functions"
  owner: "ne-postclose-trading-pipeline"
  successor: "ne-data-collection-eod (nousergon-data-PR1701, DISABLED)"
  schedule: "weekdays 16:45 America/New_York"
  runs_on: "ec2-spot"
writes:
  - "market_data/technicals/rating_history/*"
  - market_data/technicals/rating_history/_manifest.json
consumers:
  - "nousergon-data:collectors/technical_rating_ledger.py:460 (D27, in-producer derivation)"
consumers_reason: >-
    The audit scored this ABSENT (zero external readers). The plan's disposition is to
    DECLARE D27 as the consumer with evidence, rather than leave a producer reading
    `consumers: []` when its output is in fact read one function away.
contract:
  schema: contracts/rating_ledger_entry.schema.json
  producer_test: tests/test_technical_rating_ledger.py
  consumer_pins:
    - "nousergon-data:collectors/technical_rating_ledger.py:460 (D27, in-producer derivation)"
  # P-07 (alpha-engine-config#10870): the sole declared consumer is D27, the
  # realized-performance scorer in this SAME repo/module — there is no cross-repo
  # boundary to pin a copy across. tests/test_technical_rating_ledger.py already
  # exercises that reader (its own docstring: "the realized-performance scorer"),
  # so the producer test doubles as the consumer-contract test here.
registry_rows: []
# alpha-engine-config-I10870: `technical_rating_history` was never a real
# ARTIFACT_REGISTRY artifact_id — both writes[] entries are already covered
# by the market_data/technicals/rating_history/ grandfathered path_prefix
# (ARTIFACT_REGISTRY.yaml), which is what the removed entry's own
# "(path_prefix, ...)" parenthetical was pointing at without ever resolving.
run_manifest_prefix: data_collection/runs/D26
detectors:
  - kind: "freshness-monitor"
    via: "artifact-registry-row"
    state: UNVERIFIED
    note: "prefix entry; first write pending (nousergon-data-PR1698 merged 2026-09-14)"
freshness:
  family: "eod-spine"
  deadline: "18:15 America/New_York"
  status: proposed
  ratified_at: "phase-3 exit (plan §2 row 1)"
completeness:
  denominator: metron/holdings_universe.json
  floor: 0.9
  status: proposed
guards:
  empty_fresh:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  cardinality:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  units:
    state: present
    note: rating_ledger_entry.schema.json
  pit:
    state: present
    note: "length-mismatch guard (technical_rating_ledger.py:289)"
  vendor_fallback:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  success_without_output:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
audit:
  source: "alpha-engine-config/private-docs/data_collection_audit_260914.md §3"
  baseline_date: "2026-09-14"
  cells:
    identity: PRESENT
    consumers: ABSENT
    schema_contract: PRESENT
    artifact_registry: PRESENT
    observability_row: ABSENT
    run_record: PRESENT
    detector: UNVERIFIED
    console_entity: UNVERIFIED
    survives_phase4: BROKEN
clause_phase:
  identity: 2
  consumers: 1
  schema_contract: 1
  artifact_registry: 1
  observability_row: 1
  run_record: 1
  detector: 3
  console_entity: 3
  survives_phase4: 1
