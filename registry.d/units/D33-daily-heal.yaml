# Unit descriptor — data collector unit D33 (daily-heal).
#
# `console-policy` §2.6: onboarding a module is writing THIS FILE, committed
# beside the thing it describes and pointing at where its facts ALREADY live.
# Nothing about the console changes to render this unit.
#
# `data_collection_plan_260914.md` §4.1: `data_gate/clauses.py` GENERATES every
# clause from these descriptors. Nothing is hand-listed (`observability-policy`
# §2.2) — a hand-maintained monitored-things list drifts, and its drift is
# invisible because the missing rows produce no signal.
#
# RED BY DEFAULT. The `audit.cells` block below is the 2026-09-14 baseline
# reading, transcribed from alpha-engine-config/private-docs/data_collection_audit_260914.md §3. It is NOT evidence: every cell, including
# the ones scored PRESENT, is a clause that is RED until the gate measures it,
# and a first reading that DISAGREES with its cell is itself a finding — either
# the audit was wrong or the system has drifted since.
#
# PUBLIC REPO. This file carries key templates, triggers and consumer paths that
# are already public in this repository's own code. IAM role names, alarm
# thresholds and runbook detail stay in `nous-ergon-ops` (plan §5).
unit_id: D33
component_id: "data-collector-d33-daily-heal"
owning_repo: "nousergon-data"
title: "daily-heal"
component: 1
lifecycle: disabled
lifecycle_reexam: >-
    2026-09-09 — OVERDUE, which is itself why this row renders UNREPORTED rather than
    DISABLED (plan §4.1 rendering table)
owner: brian
code_path: "weekly_collector.py:2240"
trigger:
  kind: "eventbridge-rule"
  owner: "alpha-engine-daily-heal"
  successor: "ne-data-collection-daily-heal (nousergon-data-PR1701, DISABLED)"
  schedule: "cron(0 9 ? * MON-FRI *) — DISABLED live"
  runs_on: "ec2-spot"
writes:
  - "data/heal/daily/{date}.json"
  - "staging/daily_closes/*"
  - arcticdb/universe (library)
consumers: []
consumers_reason: No reader of the heal record.
consumers_decision:
  # alpha-engine-config-I10873: renders the consumers clause UNCONNECTED (not MET, not red,
  # graded by no gate). Delete this block in the same change that adds a surviving consumer.
  decision: kept-unconnected
  ruled_by: Brian
  ruled_on: "2026-09-14"
  ruling: >-
    Brian 2026-09-14 R5(a) (alpha-engine-config-I10748, plan §7); restated 2026-09-15 alpha-engine-config-I10873
  reason: >-
    kept, reincorporation planned — daily-heal is enabled at data phase 2 (after the completeness guards exist); its reader will be the phase-2 heal verification step closing detect -> act -> verify.
  reexam: >-
    data phase 2 heal verifier lands as this key's consumer (add it, delete this block)
contract:
  schema: null
  producer_test: null
  consumer_pins: []
registry_rows:
  - daily_heal_summary
run_manifest_prefix: data_collection/runs/D33
detectors:
  - kind: "cloudwatch-alarm"
    via: "alpha-engine-daily-heal-days-healed"
    state: BROKEN
    note: an alarm over a producer that NEVER RUNS. A detector that cannot fire is not a detector.
freshness:
  na_code: "N/A-NOT-RUN"
  note: >-
      the rule is DISABLED live; freshness on a disabled producer would be a permanent
      false red
  status: not_applicable
completeness:
  denominator: null
  floor: null
  status: not_applicable
  na_code: "N/A-NOT-RUN"
  note: disabled
guards:
  empty_fresh:
    state: not_applicable
    na_code: "N/A-NOT-RUN"
    note: DISABLED live; guards commission at P2 if R5 enables it
  cardinality:
    state: not_applicable
    na_code: "N/A-NOT-RUN"
    note: DISABLED live; guards commission at P2 if R5 enables it
  units:
    state: not_applicable
    na_code: "N/A-NOT-RUN"
    note: DISABLED live; guards commission at P2 if R5 enables it
  pit:
    state: not_applicable
    na_code: "N/A-NOT-RUN"
    note: DISABLED live; guards commission at P2 if R5 enables it
  vendor_fallback:
    state: not_applicable
    na_code: "N/A-NOT-RUN"
    note: DISABLED live; guards commission at P2 if R5 enables it
  success_without_output:
    state: not_applicable
    na_code: "N/A-NOT-RUN"
    note: DISABLED live; guards commission at P2 if R5 enables it
audit:
  source: "alpha-engine-config/private-docs/data_collection_audit_260914.md §3"
  baseline_date: "2026-09-14"
  cells:
    identity: PRESENT
    consumers: ABSENT
    schema_contract: ABSENT
    artifact_registry: PRESENT
    observability_row: BROKEN
    run_record: PRESENT
    detector: BROKEN
    console_entity: PRESENT
    survives_phase4: BROKEN
clause_phase:
  identity: 2
  consumers: 1
  schema_contract: 1
  artifact_registry: 1
  observability_row: 1
  run_record: 1
  detector: 3
  console_entity: 3
  survives_phase4: 1
notes: "Ruling R5. `DailyHealState` stays disabled through the cutover (plan §6.2 step 6)."
