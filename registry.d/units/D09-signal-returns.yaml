# Unit descriptor — data collector unit D09 (signal_returns).
#
# `console-policy` §2.6: onboarding a module is writing THIS FILE, committed
# beside the thing it describes and pointing at where its facts ALREADY live.
# Nothing about the console changes to render this unit.
#
# `data_collection_plan_260914.md` §4.1: `data_gate/clauses.py` GENERATES every
# clause from these descriptors. Nothing is hand-listed (`observability-policy`
# §2.2) — a hand-maintained monitored-things list drifts, and its drift is
# invisible because the missing rows produce no signal.
#
# RED BY DEFAULT. The `audit.cells` block below is the 2026-09-14 baseline
# reading, transcribed from alpha-engine-config/private-docs/data_collection_audit_260914.md §3. It is NOT evidence: every cell, including
# the ones scored PRESENT, is a clause that is RED until the gate measures it,
# and a first reading that DISAGREES with its cell is itself a finding — either
# the audit was wrong or the system has drifted since.
#
# PUBLIC REPO. This file carries key templates, triggers and consumer paths that
# are already public in this repository's own code. IAM role names, alarm
# thresholds and runbook detail stay in `nous-ergon-ops` (plan §5).
unit_id: D09
component_id: "data-collector-d09-signal-returns"
owning_repo: "nousergon-data"
title: signal_returns
component: 1
lifecycle: "retired"
owner: brian
code_path: "collectors/signal_returns.py:79"
retirement:
  ruling: "Brian 2026-09-14 R7 (alpha-engine-config-I10748, plan §7 (b))"
  reason: >-
    zero-surviving-consumer artifact — its declared reader
    (crucible-research:evals) resolves to a v1 research.db table reader with
    no v2 or Metron consumer, and its own INPUTS (predictor/predictions,
    signals/) are v1 artifacts retired in v2 phase 4; v2 attribution
    supersedes it (plan §8.1 exclusion, R7(b)).
  archive_prefix: "research.db::score_performance"
  archive_note: >-
    Retiring a producer never deletes its archive (standing preference: retain
    all archives — data is an asset). No table-drop action taken.
  no_successor_workload: true
trigger:
  kind: "step-functions"
  owner: "ne-weekly-freshness-pipeline:DataPhase1"
  successor: "RETIRED (Brian 2026-09-14 R7) — no successor workload; see retirement: above"
  schedule: "Sat 05:00 America/New_York"
  runs_on: "ec2-spot"
writes:
  - "research.db::score_performance"
consumers: []
consumers_reason: "No surviving reader. R7(b) RETIRE (alpha-engine-config-I10870, mirrors D40/D41)."
contract:
  schema: null
  producer_test: null
  consumer_pins: []
registry_rows: []
run_manifest_prefix: data_collection/runs/D09
detectors: []
freshness:
  family: weekly
  deadline: "Sat 13:00 America/New_York"
  status: proposed
  ratified_at: "phase-3 exit (plan §2 row 1)"
completeness:
  denominator: null
  floor: null
  status: not_applicable
  na_code: "N/A-MISSING-INPUT"
  note: "its inputs (predictor/predictions, signals/) are v1 artifacts"
guards:
  empty_fresh:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  cardinality:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  units:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  pit:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  vendor_fallback:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  success_without_output:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
audit:
  source: "alpha-engine-config/private-docs/data_collection_audit_260914.md §3"
  baseline_date: "2026-09-14"
  cells:
    identity: PRESENT
    consumers: PRESENT
    schema_contract: ABSENT
    artifact_registry: ABSENT
    observability_row: ABSENT
    run_record: PRESENT
    detector: ABSENT
    console_entity: ABSENT
    survives_phase4: BROKEN
clause_phase:
  identity: 2
  consumers: 1
  schema_contract: 1
  artifact_registry: 1
  observability_row: 1
  run_record: 1
  detector: 3
  console_entity: 3
  survives_phase4: 1
notes: >-
    alpha-engine-config-I10870 + R7(b) retire, RECORDED (see retirement: block above).
    Plan §8.1 EXCLUSION: its inputs are v1 artifacts retired in v2 phase 4 and v2
    attribution supersedes it. The row stays on the board so the retirement is visible
    rather than silent (mirrors D15L/D40/D41's already-executed R7 retirements).

    alpha-engine-config-I10753 (2026-09-17): D09 was ALSO still named in the
    `data-collection-weekly` schedule's `verify_units`
    (`infrastructure/cloudformation/nousergon-data-collection.yaml`) — nousergon-data-PR1701
    wrote that input before the R7 ruling landed, so the standalone stack was asserting a
    completeness claim for a producer this block declares retired with
    `no_successor_workload: true`. It read green only because
    `weekly_collector.py --phase 1` still runs the `signal_returns` phase
    (`run_units.PHASE_UNITS[("phase1", "signal_returns")]`), and would have gone RED the
    day that stops — a false red over a deliberate retirement, the
    alpha-engine-config-I7715 class. Removed, and
    `tests/test_data_collection_stack.py::test_every_standalone_successor_unit_is_covered_or_retired`
    now refuses any unit with a `retirement:` block appearing in a schedule's
    `verify_units`, so it cannot come back by hand.
