# Unit descriptor — data collector unit D10 (fundamentals).
#
# `console-policy` §2.6: onboarding a module is writing THIS FILE, committed
# beside the thing it describes and pointing at where its facts ALREADY live.
# Nothing about the console changes to render this unit.
#
# `data_collection_plan_260914.md` §4.1: `data_gate/clauses.py` GENERATES every
# clause from these descriptors. Nothing is hand-listed (`observability-policy`
# §2.2) — a hand-maintained monitored-things list drifts, and its drift is
# invisible because the missing rows produce no signal.
#
# RED BY DEFAULT. The `audit.cells` block below is the 2026-09-14 baseline
# reading, transcribed from alpha-engine-config/private-docs/data_collection_audit_260914.md §3. It is NOT evidence: every cell, including
# the ones scored PRESENT, is a clause that is RED until the gate measures it,
# and a first reading that DISAGREES with its cell is itself a finding — either
# the audit was wrong or the system has drifted since.
#
# PUBLIC REPO. This file carries key templates, triggers and consumer paths that
# are already public in this repository's own code. IAM role names, alarm
# thresholds and runbook detail stay in `nous-ergon-ops` (plan §5).
unit_id: D10
component_id: "data-collector-d10-fundamentals"
owning_repo: "nousergon-data"
title: fundamentals
component: 1
lifecycle: "in-service"
owner: brian
code_path: "collectors/fundamentals.py:526"
trigger:
  kind: "step-functions"
  owner: "ne-weekly-freshness-pipeline:DataPhase1"
  successor: "ne-data-collection-weekly (nousergon-data-PR1701, DISABLED)"
  schedule: "Sat 05:00 America/New_York"
  runs_on: "ec2-spot"
writes:
  - "archive/fundamentals/{date}.json"
consumers:
  - "crucible-research:agents/sector_teams/quant_tools.py"
  - "crucible-dashboard:health_checker.py"
contract:
  schema: "contracts/fundamentals_snapshot.schema.json"
  producer_test: tests/test_fundamentals_snapshot_contract.py
  consumer_pins:
    - repo: "crucible-dashboard"
      path: "tests/contracts/fundamentals_snapshot_s3_key_template.json"
      pins: "key_template"
      read_site: "health_checker.py::_find_latest_prefix"
      note: "KEY-TEMPLATE pin, not the body schema: health_checker.py's `fundamentals` freshness check reads only the object key's embedded date, never the body"
  # P-07 (alpha-engine-config-I10873, I10934): D10 is CONNECTED
  # (crucible-dashboard health_checker.py's `fundamentals` freshness check
  # genuinely reads `archive/fundamentals/` — verified 2026-09-16, NOT the
  # stale price_cache_slim reference the issue's remainder table named for
  # D03/D10; that claim does not hold for D10). Producer side built the
  # I10873 PR: a versioned schema for the flat ticker-keyed snapshot + a
  # producer test against the REAL `collect()` write body (both the
  # real-data and NEUTRAL sentinel shapes) and a hand-built fixture.
  #
  # CORRECTED 2026-09-21 (same I10934 session): the first companion
  # crucible-dashboard PR pinned a byte-copy of THIS FILE's schema (the
  # BODY shape). That was half a contract — health_checker.py has no
  # `get_object`/`_fetch_s3_json` call for this prefix, so a body change
  # cannot break it; the S3 key template / date format is what can. The
  # pin above now points at a key-template fixture instead
  # (`archive/fundamentals/{date}.json`, byte-copied from this
  # descriptor's own `writes` field below) and crucible-dashboard's
  # contract test builds keys from it and drives the real
  # `_find_latest_prefix` reader.
  #
  # A genuine BODY reader of this schema DOES exist —
  # crucible-research's `agents/sector_teams/quant_tools.py::
  # read_fundamentals_from_s3` (`get_object` + `json.loads`, verified
  # against that repo's origin/main 2026-09-21) — but pinning it is out
  # of scope for this PR (a crucible-research change, not a
  # nousergon-data or crucible-dashboard one); filed as
  # alpha-engine-config-I11276. `consumer_pins` above will gain a second
  # entry once that PR lands — the list is additive, not exclusive.
registry_rows:
  - fundamentals_snapshot
run_manifest_prefix: data_collection/runs/D10
detectors:
  - kind: "freshness-monitor"
    via: "artifact-registry-row"
    state: PRESENT
freshness:
  family: weekly
  deadline: "Sat 13:00 America/New_York"
  status: proposed
  ratified_at: "phase-3 exit (plan §2 row 1)"
completeness:
  denominator: "constituents - declared exclusions"
  floor: 0.9
  status: proposed
guards:
  empty_fresh:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  cardinality:
    state: report_only
    note: >-
        _FIELD_COLLAPSE_MIN_TICKERS gates a collapse REPORT, not a halt
        (collectors/fundamentals.py:716). A report is not a guard.
  units:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  pit:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  vendor_fallback:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
  success_without_output:
    state: absent
    note: >-
        no code halts or refuses on this class for this unit (audit §4.1). The guard is
        phase-2 work; the clause is UNMET until it exists and is commissioned by an
        induced fault (P-19).
audit:
  source: "alpha-engine-config/private-docs/data_collection_audit_260914.md §3"
  baseline_date: "2026-09-14"
  cells:
    identity: PRESENT
    consumers: PRESENT
    schema_contract: ABSENT
    artifact_registry: PRESENT
    observability_row: ABSENT
    run_record: PRESENT
    detector: PRESENT
    console_entity: PRESENT
    survives_phase4: BROKEN
clause_phase:
  identity: 2
  consumers: 1
  schema_contract: 1
  artifact_registry: 1
  observability_row: 1
  run_record: 1
  detector: 3
  console_entity: 3
  survives_phase4: 1
