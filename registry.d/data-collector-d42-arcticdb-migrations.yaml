# Observability registry row — GENERATED, do not hand-edit.
#
# Produced by `scripts/gen_observability_rows.py` from
# `registry.d/units/D42-*.yaml` (alpha-engine-config-I10775, plan §4.1
# item P-08). Re-run the generator after editing the unit descriptor; a diff
# here with no matching unit-descriptor change is itself a finding.
#
# Gathered by `nous-ergon-ops`'s `gather_repo_descriptors.py` (console-policy
# §2.6 one-file onboarding) — no console or ops-side code change is needed for
# this row to publish.
component_id: data-collector-d42-arcticdb-migrations
owning_repo: nousergon-data
substrate: github-actions
origin: .github/workflows/run-arctic-migrations.yml, on push to migrations/** and workflow_dispatch
owner: brian
lifecycle: in-service
authority_tier: t1
authority_tier_reason: 'A deterministic collector with no judgment: D42''s own code path (scripts/run_arctic_migrations.py;
  infrastructure/lambdas/arctic-migration-dispatcher/index.py:603) writes only the key templates its unit
  descriptor names in writes[]. No branching on model output; no LLM call in this unit.'
signals:
  execution:
    status: emitted
    reason: The owning substrate (Step Functions / GitHub Actions) retains per-execution start/terminal-status/history
      natively.
  cost:
    status: unknown
    reason: No run-level cost record exists yet; component=data-collection cost tagging is phase-3 work
      (plan P-21). Makes no LLM call.
  resource:
    status: unknown
    reason: Spot-vs-on-demand and box headroom are not independently published per unit; read at the pipeline/dispatcher
      level only.
  data:
    status: unknown
    reason: unit descriptor's own audit.cells.run_record reads ABSENT; not independently measured by this
      row. P-06 (run manifest lift) / P-19 (guard commissioning) close it.
  outcome:
    status: emitted
    reason: unit descriptor's own audit.cells.detector reads PRESENT (baseline_date 2026-09-14); re-measured,
      not re-asserted, at the next data-gate read.
log_location: github-actions:nousergon/nousergon-data/.github/workflows/run-arctic-migrations.yml
log_location_reason: Declared from the unit descriptor's trigger.detail workflow path.
alert_channel: notify-main-failure -> nousergon-lib's shared notify-ci-failure.yml
alert_channel_reason: Declared from the workflow's own notify-main-failure job, mirroring every other
  scheduled workflow in this repo.
severity_source: binary — the owning pipeline's/workflow's own terminal status
severity_source_reason: 'Declared from the trigger: no envelope, no severity string at this layer.'
console_surface: unknown
console_surface_reason: No pane renders this unit individually yet. plan §4.1's data-collection-board
  (P-03) is the console surface once built; this row is what it will render.
retention: GitHub's default 90-day Actions log retention
retention_reason: Declared from the unit descriptor's trigger; not independently probed against live bucket/log-group
  lifecycle rules for D42 specifically.
