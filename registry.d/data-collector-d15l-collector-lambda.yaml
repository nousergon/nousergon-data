# Observability registry row — GENERATED, do not hand-edit.
#
# Produced by `scripts/gen_observability_rows.py` from
# `registry.d/units/D15L-*.yaml` (alpha-engine-config-I10775, plan §4.1
# item P-08). Re-run the generator after editing the unit descriptor; a diff
# here with no matching unit-descriptor change is itself a finding.
#
# Gathered by `nous-ergon-ops`'s `gather_repo_descriptors.py` (console-policy
# §2.6 one-file onboarding) — no console or ops-side code change is needed for
# this row to publish.
component_id: data-collector-d15l-collector-lambda
owning_repo: nousergon-data
substrate: ec2-spot
origin: aws lambda invoke
owner: brian
lifecycle: deprecated
lifecycle_owner: brian
lifecycle_reexam: '2026-09-28'
lifecycle_reason: 'Generated from registry.d/units/D15L-*.yaml (P-08): the unit descriptor declares lifecycle:
  ''deprecated'' — transcribed verbatim.'
authority_tier: t1
authority_tier_reason: 'A deterministic collector with no judgment: D15L''s own code path (lambda/handler.py)
  writes only the key templates its unit descriptor names in writes[]. No branching on model output; no
  LLM call in this unit.'
signals:
  execution:
    status: unknown
    reason: No durable per-execution record independently confirmed for this trigger kind yet.
  cost:
    status: unknown
    reason: No run-level cost record exists yet; component=data-collection cost tagging is phase-3 work
      (plan P-21). Makes no LLM call.
  resource:
    status: unknown
    reason: Spot-vs-on-demand and box headroom are not independently published per unit; read at the pipeline/dispatcher
      level only.
  data:
    status: unknown
    reason: unit descriptor's own audit.cells.run_record reads UNVERIFIED; not independently measured
      by this row. P-06 (run manifest lift) / P-19 (guard commissioning) close it.
  outcome:
    status: unknown
    reason: unit descriptor's own audit.cells.detector reads UNVERIFIED; not independently measured by
      this row. P-06 (run manifest lift) / P-19 (guard commissioning) close it.
log_location: unknown
log_location_reason: No log location independently resolved for this trigger kind yet.
alert_channel: unknown
alert_channel_reason: No scheduled execution of its own to fail on; a downstream freshness row is the
  backstop.
severity_source: binary — the owning pipeline's/workflow's own terminal status
severity_source_reason: 'Declared from the trigger: no envelope, no severity string at this layer.'
console_surface: unknown
console_surface_reason: No pane renders this unit individually yet. plan §4.1's data-collection-board
  (P-03) is the console surface once built; this row is what it will render.
retention: 90 days (the owning pipeline's log group RetentionInDays); published S3 artifacts are retained
  indefinitely per the standing archive-retention preference.
retention_reason: Declared from the unit descriptor's trigger; not independently probed against live bucket/log-group
  lifecycle rules for D15L specifically.
