# boto3 is provided by the Lambda python3.12 runtime; pinned for reproducible builds.
boto3>=1.34.0
# nousergon_lib.spot_dispatch (config#2106) wraps krepis.ec2_spot (the
# spot-launch capacity-resilience chokepoint) — same pin as the other
# spot_dispatch-based dispatchers in this repo (sf-watch-spot-dispatcher,
# ci-watch-dispatcher). No [flow-doctor] extra needed — this Lambda sends no
# Telegram of its own (see index.py docstring: the on-box runner sends the
# outcome-specific receipt once the migration concludes).
nousergon-lib @ git+https://github.com/nousergon/nousergon-lib@v0.124.5
krepis>=0.14.0
