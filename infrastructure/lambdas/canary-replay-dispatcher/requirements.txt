# boto3 is provided by the Lambda python3.12 runtime; pinned for reproducible builds.
boto3>=1.34.0
# nousergon_lib.spot_dispatch (config#2106) — same pin as ci-watch-dispatcher/
# sf-watch-spot-dispatcher. v0.122.0 is the first version whose
# launch_with_fallback accepts extra_tags (config#2292 root fix for
# config#2267 site 2 / config#2836): the run_token discriminator tag now
# rides the SAME RunInstances call as the launch itself, closing the
# dedupe-guard TOCTOU window a separate post-launch create_tags call left
# open. EXEMPTED from root-pin lockstep in tests/test_lib_pin_lockstep.py
# (same contract reason as ci-watch-dispatcher/sf-watch-spot-dispatcher).
nousergon-lib @ git+https://github.com/nousergon/nousergon-lib@v0.122.0
krepis>=0.12.0
