# boto3 is provided by the Lambda python3.12 runtime; pinned for reproducible builds.
boto3>=1.34.0
# alpha-engine-config-I9329 — the shared nousergon_lib.spot_dispatch chokepoint
# (launch_with_fallback + wait_ssm_online + send_async_command +
# terminate_on_failure). Pinned in LOCKSTEP with this repo's root
# requirements.txt (tests/test_lib_pin_lockstep.py) rather than carrying its own
# exemption: this Lambda depends on no feature ahead of what root already pins.
nousergon-lib @ git+https://github.com/nousergon/nousergon-lib@v0.124.150
# krepis.spot_bootstrap.render_bootstrap() renders this box's watchdog units,
# hard-runtime timer, interpreter install and public clone. Same lockstep rule:
# a floor 45 minors below what the handler imports is a pin that says nothing
# and fails on nothing (alpha-engine-config-I7372).
krepis==0.59.67  # bumped 0.59.54 -> 0.59.65: alpha-engine-config-I11359, RunLog(gzip=True) (krepis-PR229). Prior history: bumped 0.59.41 -> 0.59.54: nousergon-lib v0.124.116 (root requirements.txt) floors krepis>=0.59.52; alpha-engine-config-I10226 lockstep gap, run 34309659890 Bumped 0.59.65 -> 0.59.66 for alpha-engine-config-I11273 (krepis-PR230): _build_run_instances_kwargs now emits a SECOND TagSpecifications entry with ResourceType 'volume' carrying the same merged tags as the instance entry, so the 60GB EBS volume every spot launch creates is no longer invisible to every cost-allocation-tag CUR query. Library-level, so every caller of krepis.ec2_spot.launch() gets it through this one pin -- but ONLY once the pin moves, which is what this commit is.
