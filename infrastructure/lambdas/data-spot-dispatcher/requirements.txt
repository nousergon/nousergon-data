# boto3 is provided by the Lambda python3.12 runtime; pinned for reproducible builds.
boto3>=1.34.0
# config#1767 — ec2_spot launch chokepoint (SAME pin as scheduled-groom-dispatcher).
nousergon-lib @ git+https://github.com/nousergon/nousergon-lib@v0.83.0
krepis>=0.10.2
