schema_version: 1
# Allowlist for `tests/test_no_debug_only_swallows.py` — every entry below is
# a debug-only-or-pass `except Exception` swallow detected by
# `nousergon_lib.testing.debug_swallow_guard`, individually triaged and
# carved out for the reason named. This repo's own AGENTS.md states there is
# NO graceful-degrade carve-out for a WRITER here — every entry below is
# either a genuinely non-data-bearing cosmetic path, or an already-documented
# in-band fallback whose ultimate failure state is recorded loud elsewhere in
# the same function. Any swallow found on a write path was fixed (RAISE or
# RECORD-LOUD), never allowlisted — see alpha-engine-config-I10226.
entries:
  - path: collectors/alternative.py
    line: 1634
    reason: >-
      (a) failure mode swallowed: s3.get_object against
      archive/revisions/{date}.json fails/404s for every checked date.
      (c) no recording surface needed — the comment immediately above this
      block (line ~1549) already establishes no writer of that S3 key exists
      anywhere in this codebase, so the loop 404s by construction; it is
      documented dead code kept only as a non-regressing safety net, and
      result["revision_4w"] is already None on entry and stays None, so
      nothing is silently degraded from a populated value. (Shifted +8 by
      alpha-engine-config-I10899's `from dates import ...` bounded-fetch
      import addition; site unchanged.) (Shifted +1 by alpha-engine-config-
      I11216's `from shadow.root import active_root` import addition; site
      unchanged.)
    expires: "2027-03-10"
    tracking: alpha-engine-config-I10226
  - path: emailer.py
    line: 44
    reason: >-
      (a) failure mode swallowed: results["started_at"]/["completed_at"]
      missing or unparseable. (c) no recording surface — this only formats a
      cosmetic duration substring for the notification email subject/body;
      it writes no data and feeds no downstream consumer, so a blank
      duration_str is the correct degraded rendering, not a corruption.
    expires: "2027-03-10"
    tracking: alpha-engine-config-I10226
  - path: features/compute.py
    line: 940
    reason: >-
      (a) failure mode swallowed: the exact-date S3 fundamentals key missing
      or unreadable. (c) recording surface: none needed at this step —
      control falls through to the "scan for most recent fundamentals file"
      fallback a few lines below, whose own failure IS recorded loud
      (log.warning at ~line 911) and whose total-absence outcome is ALSO
      recorded (log.info at ~line 913). Read-side cache lookup, not a
      writer — nothing is persisted here. (Shifted +47 by alpha-engine-config-I10923's ReferenceMapUnavailable class + the two loaders' raise-on-failure rewrite; site unchanged.)
    expires: "2027-03-10"
    tracking: alpha-engine-config-I10226
  - path: weekly_collector.py
    line: 3171
    reason: >-
      (a) failure mode swallowed: results["started_at"]/["completed_at"]
      missing or unparseable. (c) no recording surface — cosmetic duration
      suffix for the logger.info summary line immediately below; writes no
      data, feeds no downstream consumer. (Line shifted from 2754 by
      alpha-engine-config-I9288's `assert_valid_price_cache_ticker` import +
      call additions (2 lines); site unchanged.) (Shifted +6 by alpha-engine-config-I10891's shadow run-state import + skip guard.) (Shifted +1 by alpha-engine-config-I10893's chronic-gap-heal future-bar guard.) (Shifted +20 by alpha-engine-config-I10880's shadow-flow-name setup_logging override.) (Shifted +28 by alpha-engine-config-I10904's CaretTickerError import, guard-call wrap and the except-clauses on the chronic-gap self-heal write site.) (Shifted +43 by alpha-engine-config-I11026's `_prices_extra_outputs` helper, the dict-shaped `rows_fn` extension to `_record_phase_lineage`'s extra_outputs loop, and the two `extra_outputs=` call-site additions on the `prices` phase; site unchanged.) (Shifted +6 by alpha-engine-config-I10941's bounded `describe_mode_failure` comment on the whole-mode `_CollectorError` raise; site unchanged.) (Shifted +57 by alpha-engine-config-I10942's morning_daily_closes phase-honesty fix — the target-driven status check, artifact-key recording, and `_daily_closes_written_keys` helper; site unchanged.) (Shifted +76 by alpha-engine-config-I11230's `_KNOWN_COLLECTOR_STATUSES` closed-vocabulary constant, the `_phase_body` exhaustive-status check, the `_DegradedRun`/`_record_phase_lineage` "partial" routing, and their doc comments; site unchanged.)
    expires: "2027-03-10"
    tracking: alpha-engine-config-I10226
  - path: weekly_collector.py
    line: 4545
    reason: >-
      (a) failure mode swallowed: results["started_at"]/["completed_at"]
      missing or unparseable. (c) no recording surface — cosmetic duration
      suffix for the logger.info summary line immediately below; writes no
      data, feeds no downstream consumer. (Line shifted from 4123 by
      alpha-engine-config-I9288's `assert_valid_price_cache_ticker` import +
      call additions (2 lines); site unchanged.) (Shifted +6 by alpha-engine-config-I10891's shadow run-state import + skip guard.) (Shifted +1 by alpha-engine-config-I10893's chronic-gap-heal future-bar guard.) (Shifted +20 by alpha-engine-config-I10880's shadow-flow-name setup_logging override.) (Shifted +28 by alpha-engine-config-I10904's CaretTickerError import, guard-call wrap and the except-clauses on the chronic-gap self-heal write site.) (Shifted +48 by alpha-engine-config-I11026's `_prices_extra_outputs` helper, the dict-shaped `rows_fn` extension to `_record_phase_lineage`'s extra_outputs loop, and the two `extra_outputs=` call-site additions on the `prices` phase; site unchanged.) (Shifted +6 by alpha-engine-config-I10941's bounded `describe_mode_failure` comment on the whole-mode `_CollectorError` raise; site unchanged.) (Shifted +57 by alpha-engine-config-I10942's morning_daily_closes phase-honesty fix — the target-driven status check, artifact-key recording, and `_daily_closes_written_keys` helper; site unchanged.) (Shifted +76 by alpha-engine-config-I11230's `_KNOWN_COLLECTOR_STATUSES` closed-vocabulary constant, the `_phase_body` exhaustive-status check, the `_DegradedRun`/`_record_phase_lineage` "partial" routing, and their doc comments; site unchanged.)
    expires: "2027-03-10"
    tracking: alpha-engine-config-I10226
  - path: weekly_collector.py
    line: 4842
    reason: >-
      (a) failure mode swallowed: results["started_at"]/["completed_at"]
      missing or unparseable. (c) no recording surface — cosmetic duration
      suffix for the logger.info summary line immediately below; writes no
      data, feeds no downstream consumer. (Line shifted from 4284 by
      alpha-engine-config-I9288's `assert_valid_price_cache_ticker` import +
      call additions (2 lines); site unchanged.) (Shifted +6 by alpha-engine-config-I10891's shadow run-state import + skip guard.) (Shifted +1 by alpha-engine-config-I10893's chronic-gap-heal future-bar guard.) (Shifted +20 by alpha-engine-config-I10880's shadow-flow-name setup_logging override.) (Shifted +28 by alpha-engine-config-I10904's CaretTickerError import, guard-call wrap and the except-clauses on the chronic-gap self-heal write site.) (Shifted +48 by alpha-engine-config-I11026's `_prices_extra_outputs` helper, the dict-shaped `rows_fn` extension to `_record_phase_lineage`'s extra_outputs loop, and the two `extra_outputs=` call-site additions on the `prices` phase; site unchanged.) (Shifted +6 by alpha-engine-config-I10941's bounded `describe_mode_failure` comment on the whole-mode `_CollectorError` raise; site unchanged.) (Shifted +57 by alpha-engine-config-I10942's morning_daily_closes phase-honesty fix — the target-driven status check, artifact-key recording, and `_daily_closes_written_keys` helper; site unchanged.) (Shifted +76 by alpha-engine-config-I11230's `_KNOWN_COLLECTOR_STATUSES` closed-vocabulary constant, the `_phase_body` exhaustive-status check, the `_DegradedRun`/`_record_phase_lineage` "partial" routing, and their doc comments; site unchanged.) (Shifted +136 by alpha-engine-config-I11444's `_seed_index_entrants` / `_run_entrant_seed_step` helpers and their EOD-append call site; site unchanged.)
    expires: "2027-03-10"
    tracking: alpha-engine-config-I10226
