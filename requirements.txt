boto3>=1.43.91
pandas>=2.0
openpyxl>=3.1.5
pyarrow>=25.0.1
yfinance>=1.7.0
requests>=2.34.2
pyyaml>=6.0.3
# setuptools floor for PYSEC-2026-3447 (newly disclosed; CI's pip-audit gate
# started failing 2026-07-14 against the default 79.0.1 bundled by the
# GH Actions Python 3.11 image). Not a build-backend requirement here — direct
# pin exists solely to satisfy the security-audit gate.
setuptools>=84.0.0
# Bitcoin HD-wallet (xpub/ypub/zpub) derivation for the crypto-balances producer
# (collectors/crypto_balances.py). Pure-Python-capable (ships a prebuilt linux libsecp256k1
# + a pure fallback) → Lambda-safe, no platform wheels. metron-ops#111.
embit>=0.8.0
numpy>=2.4.6
feedparser>=6.0.14
beautifulsoup4>=4.15.0
lxml>=6.1.3
edgartools>=5.57.0
voyageai>=0.5.0
jsonschema>=4.26.0
arcticdb>=6.25.0
# flow-doctor is pulled in transitively via nousergon-lib[flow-doctor].
# NOTE: the extra MUST be hyphenated — setuptools normalizes the lib's
# `flow_doctor` extra to `Provides-Extra: flow-doctor`; older pip doesn't
# normalize underscore->hyphen, so `[flow_doctor]` silently drops it (config#1178).
# psycopg2-binary + pgvector now come via [rag] (added in lib v0.3.0,
# direct pins dropped 2026-05-20 after >2-week soak on v0.20.0).
# v0.30.0 + v0.31.0 add the cost-telemetry SDK adapter
# (``metadata_from_anthropic_message``) + server-tool fee tracking
# (``ToolFee`` / ``ToolFeeTable``). v0.33.0 adds the
# ``record_anthropic_call`` capture chokepoint used by
# ``rag/pipelines/_cost_telemetry.py`` to wire
# ``collectors/nlp/event_extraction.py``'s news-article event-extraction
# LLM calls into the cost-telemetry stream (highest-volume previously-
# untracked site at ~100–300 Haiku calls/wk during RAGIngestion).
# v0.38.0 adds the ``locks`` submodule
# (``universe_writer_lock`` S3-conditional-PUT primitive) consumed by
# ``builders/daily_append.py`` for the producer-side single-writer
# invariant (ROADMAP L286). Gated default-OFF via
# ``ALPHA_ENGINE_UNIVERSE_WRITER_LOCK_ENABLED``.
# All additive surface — existing imports unchanged.
#
# alpha-engine-config#2456 NOTE: builders/daily_append.py's new L2
# series-contract wiring imports nousergon_lib.series_contract, which ships
# on nousergon-lib#195 (not yet merged/tagged as of this PR). This repo's
# nousergon-lib pin MUST stay a tagged ``vX.Y.Z`` release in lockstep across
# requirements.txt / Dockerfile / requirements-daily-news.txt / Lambda
# requirements (tests/test_lib_pin_lockstep.py) — a branch ref is not a
# valid pin here, so this file intentionally still points at v0.86.0. Bump
# to (and add the ``series-contract`` extra from) the first tagged release
# that includes nousergon-lib#195 as an immediate same-repo follow-up PR
# once that release ships; until then this repo's test suite exercises the
# wiring against an editable install of the nousergon-lib branch (see this
# PR's description) rather than a real pip-installable pin.
# nousergon/alpha-engine-config#2480 NOTE: tests/test_pipeline_status_registry_source_check.py
# (added in this PR) asserts every substantive SF Task state has a
# nousergon_lib.pipeline_status.registry.STATE_TO_ARCHIVE_PAGE entry.
# nousergon-lib#226 registered the 9 states this repo's SF JSONs were
# missing (2 Saturday + 7 EOD) and shipped in v0.124.8 — bumped to that tag
# here (was v0.124.6) to unblock the new source-check test.
# alpha-engine-config-I7111: MarketHoursGate + its four notify/record states
# are new substantive Task states in BOTH weekday definitions, so the SAME
# source-check requires their registry entries to exist in the pinned lib.
# nousergon-lib-PR311 adds all five and MUST merge first; v0.124.53 was the
# patch autobump that merge produced from 0.124.52 (auto-version-bump.yml is
# the single version writer). If another lib PR merges between, this pin moves
# to whatever tag PR311's merge actually produced — the source-check is what
# tells you, and it is the guard against merging the SF JSON ahead of the
# registry entry.
#
# 2026-08-18: v0.124.53 -> v0.124.67 (alpha-engine-config-I7617). The
# freshness scan's S3 LIST capped at 8 pages / 8000 objects and, on hitting
# the cap, returned the newest object it had seen so far AS IF it were the
# newest under the prefix. S3 lists lexically and these prefixes are
# date-templated, so the unread tail is the newest part:
# market_data/weekly/ reached 8208 objects and five registry rows paged
# CRITICAL for artifacts that were fresh. nousergon-lib-PR326 makes
# truncation a probe failure and raises the cap; that fix reaches the
# freshness-monitor Lambda ONLY through this pin, which is why the bump is
# the deploy.
#
# Bumped on EVERY surface in lockstep (30 files) per
# tests/test_lib_pin_lockstep.py — the Dockerfile's hardcoded pin wins over
# requirements.txt, and requirements-daily-news.txt and
# deploy-infrastructure.yml carry their own copies. A single-file bump has
# reached production as a canary ModuleNotFoundError three times.
# (Superseded: alpha-engine-config-I7119 / nousergon-lib-PR310, merged as
# v0.124.51, which registered RelaunchWeeklyFreshnessSpot.)
#
# alpha-engine-config-I7267 NOTE: this PR's step_function.json adds two new
# substantive Task states (PitParityLookaheadResourceKillCheck /
# PitParityWalkforwardResourceKillCheck) plus their WaitFor* companions.
# tests/test_pipeline_status_registry_source_check.py therefore fails until
# the pinned nousergon-lib carries their STATE_TO_ARCHIVE_PAGE /
# WAIT_GROUPING entries — nousergon-lib-PR333 adds both and MUST merge
# first; this pin does NOT move in this PR (bumping is a lockstep 30-file
# change per tests/test_lib_pin_lockstep.py, out of scope here). Once
# PR333 merges and auto-version-bump.yml cuts the release, an immediate
# same-repo follow-up PR bumps this pin (and its 29 siblings) to that tag —
# same sequencing PR311/PR326 above already established for this exact
# class of dependency. Until then, that one source-check failure is
# EXPECTED and tracked by this note, not a silent regression.
#
# 2026-08-20: v0.124.75 -> v0.124.78 (alpha-engine-config-I7813). The
# observe-only scanner leaderboard became its own weekly-SF leaf state
# (ScannerLeaderboard + PublishScannerLeaderboardDegraded +
# NotifyCompleteScannerLeaderboardDegraded), and all three are substantive
# Task states, so tests/test_pipeline_status_registry_source_check.py fails
# until the pinned lib carries their STATE_TO_ARCHIVE_PAGE entries.
# nousergon-lib-PR<N> adds all three and MUST merge first; v0.124.78 is the
# patch autobump that merge is expected to produce from 0.124.77. If another
# lib PR merges between, this pin moves to whatever tag PR<N>'s merge actually
# produced — the source-check is what tells you, and it stays RED until the
# pin is right, which is the guard against merging the SF JSON ahead of the
# registry entry.
# alpha-engine-config-I8336: bumped 0.124.87 -> 0.124.88 for the
# PublishAggregateCostsDegraded STATE_TO_ARCHIVE_PAGE entry
# (nousergon-lib-PR353, merged first). The weekly SF gains that sns:publish
# state in this same PR, and tests/test_pipeline_status_registry_source_check.py
# stays RED until the pin carries the row — which is the guard against the SF
# JSON landing ahead of the registry entry. VERIFIED 2026-08-25 against the tag
# itself (contents API at ref=v0.124.88), not against the merge: a green publish
# that serves old metadata has happened on this fleet before.
# alpha-engine-config-I9329: bumped 0.124.97 -> 0.124.102 for
# DispatchEvalJudgeSpot + WaitForEvalJudge{SpotBootstrap,Process} registry
# entries (nousergon-lib-PR376, merged first). The Saturday SF gains those
# states in this PR; tests/test_pipeline_status_registry_source_check.py stays
# RED until the pin carries the rows — the guard against SF JSON landing ahead
# of the registry entry.
# alpha-engine-config-I10226: this repo's tests/test_no_debug_only_swallows.py
# imports nousergon_lib.testing.debug_swallow_guard, lifted in
# nousergon-lib-PR398 (branch nel-i10226-swallows) and MERGED, published as
# v0.124.115. Pinned to v0.124.116 (the current tip at PR-open time, one
# unrelated feature ahead of v0.124.115 — nousergon-lib-PR399/I10230's LLM
# endpoint probe lift) so this pin and the Lambda pins below stay in
# lockstep with the rest of the repo's already-bumped v0.124.116 dependents.
nousergon-lib[arcticdb,flow-doctor,rag,contracts] @ git+https://github.com/nousergon/nousergon-lib@v0.124.116
# krepis floor: sf_preflight.py reads the operator pricing override
# (cost/model_pricing.yaml) via nousergon_lib.cost -> krepis.cost.PriceCard,
# which is extra="forbid". config#1332 adds the cache_create_1h_per_1m field,
# present only in krepis>=0.14.0 — an older krepis rejects the YAML. Pinned
# directly (not via a nousergon-lib bump, which would drag the in-flight,
# 7/3-cutover SF-rename refs in lib 0.74.0 into this repo). Floor guarantees
# the upgrade even on a non-fresh venv.
#
# Floor raised to 0.18.8 (2026-07-27): the weekly + EOD SF definitions now pass
# `--correlation-id` to `krepis.ssm_log_capture run`, an argument that does not
# exist before 0.18.8 — an older krepis rejects it with `unrecognized
# arguments` and the stage dies at launch. The floor is what makes the
# definition's use of the flag safe; without it a non-fresh venv (verified: a
# local 0.16.2 venv still satisfied `>=0.14.0`) resolves to a krepis that
# cannot run the commands the SF emits. 0.18.8 is also the version that made
# the correlation id MANDATORY (`run` exits 2 without it), so this floor pins
# both halves of the same contract. See tests/test_sf_krepis_correlation_id.py.
# NOTE (this PR, flow-doctor-provider fix): krepis==0.59.21's own
# `flow_doctor` extra caps `flow-doctor[diagnosis,s3]` to `<0.10` — a version
# that predates RouterProvider (added 0.12.0) entirely, so the
# `diagnosis.provider: router` set below in flow-doctor.yaml is inert until
# nousergon-data-PR1456 (dependabot: krepis 0.59.12 -> 0.59.18) merges.
# 0.59.18 is the first krepis release carrying krepis-PR168, which drops the
# stale `<0.14` cap and the forced `anthropic` SDK pull. Sequence: PR1456
# before or together with this PR — see tests/test_flow_doctor_wiring.py's
# test_diagnosis_enabled_requires_explicit_provider docstring for why the
# schema-level guard does not depend on the installed flow-doctor version.
# alpha-engine-config-I10226: bumped 0.59.46 -> 0.59.53 in lockstep with the
# nousergon-lib v0.124.116 pin above — measured locally: pip resolution fails
# with "nousergon-lib 0.124.114 depends on krepis>=0.59.52" against the old
# 0.59.46 pin, and 0.59.53 was the latest published release satisfying that
# floor as of an earlier revision of this PR. Bumped again to 0.59.54 (the
# latest published release as of this commit) to pick up the get_secret fix
# (krepis-PR210) and, more importantly, to give every krepis pin in this
# repo — root plus all per-Lambda requirements.txt files — ONE consistent
# version instead of a patchwork: the lockstep bump in this PR's own diff
# had raised nousergon-lib to a version requiring krepis>=0.59.52 without
# carrying every krepis==0.59.41 Lambda pin forward, which broke `pip
# install` for those Lambdas' own requirements.txt in CI (run 34309659890).
krepis==0.59.57  # pinned per §139 — first-party/fast-moving deps are pinned, never floored; alpha-engine-config-I7009 (relaunch-decision --json), I7372 (spot_bootstrap: scan_for_inline_bootstraps + --max-runtime-seconds + extra_clones, all first shipped in 0.59.6). Bumped 0.59.6 -> 0.59.12 for krepis.aws_region.resolve_region() (krepis-PR160, alpha-engine-config-I7428) — validators/stage_output_sweep.py's stepfunctions client needs it. main was at 0.59.11 when krepis-PR160 opened; VERIFIED 2026-08-17 after that merge — autobump published exactly 0.59.12, present on both the PyPI JSON API and the simple index (separate caches; a pin chained behind a release fails with an error reading exactly like 'that version does not exist'). Bumped 0.59.12 -> 0.59.23 for krepis.aws remove-lambda-env (krepis-PR175, alpha-engine-config-I7925). Bumped 0.59.23 -> 0.59.24 (krepis-PR176, alpha-engine-config-I8030) — 0.59.23's remove-lambda-env enumerated Lambda aliases unconditionally, even under --defer-publish, and this repo's deploy role holds no lambda:ListAliases, so alpha-engine-data-collector's deploy stranded a partial deploy (image pushed, alias never moved); 0.59.24 skips the enumeration under --defer-publish. Bumped 0.59.24 -> 0.59.31 for alpha-engine-config-I8155: StageVerdict now requires non-empty execution run_date and keys only on it.; bumped 0.59.31 -> 0.59.33 for alpha-engine-config-I8298 — 0.59.33 is the first krepis whose flow-doctor extra floors flow-doctor>=0.16.0, below which TelegramNotifier.validate() catches only URLError and a TimeoutError from an unreachable api.telegram.org escapes on the IMPORT path (it killed alpha-engine-predictor-inference and degraded two postclose safety gates on 2026-08-24). 0.59.31 and 0.59.32 both still declared flow-doctor>=0.8.1 — krepis-PR184 raised it but auto-version-bump keyed on src/ only, so it merged green and published nothing (krepis-PR185). Bumped 0.59.39 -> 0.59.41 with the nousergon-lib v0.124.102 pin (lib floors krepis>=0.59.41 for krepis-PR194 router tier groups, alpha-engine-config-I9297). Bumped 0.59.41 -> 0.59.53 -> 0.59.54 with the nousergon-lib v0.124.116 pin (lib floors krepis>=0.59.52; alpha-engine-config-I10226) — every per-Lambda krepis==0.59.41 pin bumped to the same 0.59.54 in the same commit to close the lockstep gap CI caught in run 34309659890.
